using Microsoft.AspNetCore.Mvc.Rendering;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;


/// <summary>
/// Enumの拡張クラス
/// </summary>
public static class EnumExtentions_Web
{
    //public static IEnumerable<SelectListItem> AsSelectListItem<T>(this T value) where T : Enum, IConvertible
    //{
    //    if (value == null)
    //        return new List<SelectListItem>();

    //    var enumList = (T[])Enum.GetValues(value.GetType());

    //    var result = enumList.Select(e => new SelectListItem(e.GetDescription(), e.ToInt32(null).ToString()));
    //    return result;
    //}

}
